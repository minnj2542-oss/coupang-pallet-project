# 프로젝트: Coupang Supplier Hub 트럭적재(파레트) 자동구성 웹앱

## 목적
쿠팡 트럭 쉽먼트 시, 상차리스트(출고할 상품 목록)를 업로드하면 쿠팡 규격에 맞춰
파레트(PLT) 구성을 자동으로 계산해주는 단일 HTML 웹앱. (index.html 하나로 동작, 서버 없음)

## 핵심 파일
- `index.html` : 전체 앱 (HTML+CSS+JS 한 파일). localStorage로 데이터 영구저장 (2026-07-12부터, GitHub Pages 배포 대비 window.storage에서 전환). 저장 키 2개: `master-list`(재적 마스터, 2026-07-15부터 계산에 참여 — PLT 재계산 최우선 기준) / `unit-reference-list`(단위 기준표, 낱개수 계산 및 fallback에 사용). SheetJS는 더 이상 사용 안 함(파일 업로드 기능 제거, 아래 참고).

## 배포
- GitHub Pages로 배포 완료. 저장소: https://github.com/minnj2542-oss/coupang-pallet-project (main 브랜치, root에서 서빙)
- 사이트 주소: https://minnj2542-oss.github.io/coupang-pallet-project/
- 마스터리스트는 **브라우저별 localStorage**로 확정 (팀원 간 공유 안 됨). 2026-07-13에 Firebase 등 공유 저장소 전환을 검토했으나 "너무 복잡하다"는 이유로 사용자가 명시적으로 하지 않기로 결정함. 다시 요청받기 전까진 이 주제를 꺼내지 말 것.
- 코드 수정 후 배포에 반영하려면 반드시 git add/commit/push까지 해야 함 (로컬 파일만 고치면 실제 사이트엔 반영 안 됨).

## 도메인 규칙 (반드시 지킬 것) — v3 (2026-07-13 업데이트, 사용자와 대화로 확정)

1. **상차리스트(입력값)가 메인.** 상차리스트에는 이미 PLT 값이 포함되어 들어온다 (외부에서 계산되어 옴).
   열 순서 고정 10열 (2026-07-13, v3.2로 확정): **고객사&거래처명 / 상품코드 / 상품명 / BOX바코드 / 상품바코드 / BOX입수 / BOX / EA / 총낱개 / PLT수**
   - `BOX입수`(박스당 낱개수) × `BOX`(박스수) = `총낱개`. `EA`는 실사용상 항상 0에 가까운 값이라(사용자 확인) 계산에 안 씀, 그리드에만 존재.
   - 마스터 override 계산은 **BOX**(박스수)를 쓰고(단위 정합성 확보), 파레트/미리보기 등 화면 표시 수량은 **총낱개** 기준. 총낱개는 낱개가 최소 단위라 **절대 소수점이 나올 수 없음** — 파싱 시(`importShipGrid`)와 파레트 배분 시(아래 규칙 4) 둘 다 정수로 강제함.
   - `고객사&거래처명`(예: "동탄1", "이천2")은 배차번호 붙은 상차리스트에 여러 거래처가 섞여 들어올 때 쓰는 필터 키. 아래 규칙 7 참고.
   - GRID_COLS = `['vendor','code','name','b188','b88','unitsPerBox','boxQty','eaExtra','totalUnits','plt']` 순서로 내부 필드명과 매핑됨.
   - **입력 방식은 엑셀 붙여넣기 전용 스프레드시트 그리드**. 사용자 회사 환경에서 파일 업로드가 막혀 있어 파일 업로드 기능은 제거함. `#ship-grid-tbody`에 30행짜리 빈 표를 기본 제공하고, 아무 칸에 포커스 후 Ctrl+V로 엑셀 셀 여러 개를 한번에 붙여넣으면 해당 칸부터 채워지고 모자란 행은 자동으로 늘어남 (`shipGrid` 상태, paste 이벤트 위임으로 처리). "상차리스트 불러오기" 버튼을 눌러야 `fullShipment` 배열로 확정됨 (거래처 필터링 전 전체).
   - **행 전체 유효성 검사 (2026-07-15 변경, v4)**: 상차리스트 다운로드 파일 맨 아래엔 상품코드/거래처명 등이 비어있고 수량만 합산된 "합계행"이 붙어있는 경우가 있어, **10개 열 중 하나라도 빈칸이면 그 행 전체를 계산에서 제외**한다 (`isCompleteShipRow`). 예전엔 "상품코드만 있으면 통과"였는데, 합계행 유입을 막기 위해 전체 열 필수로 강화함. 병합셀로 인한 "코드 없는 연속줄"을 위쪽 행에서 이어받는 forward-fill 방식은 시도했다가 **폐기**됨 — 사용자가 "그런 빈 줄은 이어받지 말고 그냥 제외해야 한다"고 명확히 함(2026-07-15).
2. **재적 마스터리스트** (`#panel-master`, `master` 배열, `MASTER_KEY` localStorage). **2026-07-15 대전환(v4): 이제 계산에 참여함.** 기존엔 "입고 파레트→쿠팡 출고 변환"을 위한 별도 용도라 계산 미사용이었는데, 사용자가 명확히 함: 아래 3번 "단위 기준표"는 **우리 센터가 지금 쌓고 있는 방식**(자사 기준)을 나타낼 뿐이고, 실제 쿠팡에 납품하려면 **재적 마스터관리(쿠팡박스수)가 요구하는 방식으로 다시 쌓아야** 하므로, 이 표가 실제 "재적단량(PLT)" 계산의 **최우선 기준**이 된다.
   - 정확히 4열 (스키마 안 바뀜): 상품코드 / 상품명 / 박스수·PLT(자사 기준, ownBox, 참고용/계산 미사용) / 쿠팡박스수·PLT(cpBox, **PLT 재계산에 사용**)
   - `cpBox`는 "56 (8방 * 7단)"처럼 숫자+구성설명 텍스트 허용 (단위 기준표의 cpBox와 동일하게 `cpBoxNum()`으로 맨 앞 숫자만 파싱).
   - **이 표엔 "박스당 낱개수" 정보가 없음** — 완충파레트 낱개수 계산 시 박스당낱개수는 항상 단위 기준표(같은 상품코드)에서 가져와 결합해서 씀 (아래 규칙 4, 5번 참고).
3. **단위 기준표** (`#panel-unitref`, `unitRef` 배열, `UNITREF_KEY` localStorage). 회사가 취급하는 **전 상품**(~1200개)의 "PLT당 박스량"과 "박스당 낱개수"를 담는 참조 테이블. **의미: 우리 센터가 현재 쌓고 있는(자사 기준) 방식** (2026-07-15 명확화 — 재적 마스터관리의 "쿠팡 기준"과 대비됨).
   - 정확히 4열: 상품코드 / 상품명 / PLT당 박스량 / 박스당 낱개수
   - "PLT당 박스량" 칸은 "48 (8방 * 6단)"처럼 숫자+구성설명이 섞여 들어갈 수 있음. 계산 시엔 맨 앞 숫자만 파싱 (cpBoxNum 함수). "박스당 낱개수"는 순수 숫자.
   - 이 표 단독으로는 (재적 마스터관리에 없는 상품에 한해) PLT가 0/빈값일 때만 재적 복구 fallback으로 쓰이고, 그 외엔 완충파레트 낱개수 정밀 계산에만 조용히 쓰인다 (아래 규칙 4번).
   - 브라우저별 localStorage 저장, 팀 공유 안 됨 (배포 섹션 참고). ~1200행 붙여넣기 실측 300행/24ms로 성능 문제 없음 확인.
4. **PLT 결정 로직 (resolveItemPlt)** — **2026-07-15 대전환 (v4, 최종)**. 우선순위:
   1. **재적 마스터관리에 그 상품(코드/상품명)이 등록돼 있으면, 입력 PLT가 정상값이어도 무조건 `BOX ÷ 재적마스터.쿠팡박스수`로 다시 계산해서 덮어쓴다.** (2026-07-15, 사용자가 명확히 확정 — "재적단량에 한해서는 [입력 PLT를] 바꾸어야지". 이전까지의 "유효한 입력 PLT는 절대 안 건드림" 원칙은 **재적 마스터관리에 등록된 상품에 한해** 적용되지 않음.)
   2. 재적 마스터관리에 없으면 → **입력 PLT가 0보다 큰 정상값이면 그대로 사용** (기존 원칙 유지).
   3. 입력 PLT도 0/빈값/음수이면 → 단위 기준표에 등록된 상품이면 `BOX ÷ 단위기준표.PLT당박스량`으로 재적 복구.
   4. 그래도 안 되면 계산에서 제외하고 "PLT 값 누락" 카드로 경고.
   - 재적 마스터관리(1번) 또는 단위 기준표 fallback(3번)으로 값이 채워진 경우, 상차리스트 미리보기의 "비고" 열에 **보라색 "재적값 사용"** 뱃지로 표시 (2026-07-15 추가 — 이전엔 fallback 여부가 안 보이는 문제가 있었음). 순수 입력값 그대로면 초록색 "입력값 사용".
   - PLT 화면 표시는 소수 둘째 자리까지만 (`fmtPlt`, `toFixed(2)`) — 2026-07-15, 넷째 자리(1.1607)까지 나오던 걸 줄임.
5. **파레트 구성 알고리즘**: 상품별로 결정된 PLT(occupancy)를 합산해서 사용. 단위 기준표 매칭 여부와 무관하게 동일한 방식으로 처리한다.
   - **예외: 거래처명에 "XRC"가 포함되면 혼적 자체를 금지** (2026-07-15 추가). 쿠팡 XRC는 창고라 파레트에 서로 다른 상품을 섞으면 안 된다는 게 사용자 확인 사항. `runCalculation()`에서 `shipment[0].vendor.includes('XRC')`로 판단(`isXRC`), true면 `remainderPool`의 각 자투리를 곧바로 `{type:'leftover', fill:occupancy, items:[item]}` 단일 파레트로 만들고, 아래의 큰것/작은것 배치·병합·재배분 로직 전체를 건너뛴다. XRC가 아닌 거래처는 기존 로직 그대로.
   - 상품별 총 PLT(occupancy)에서 정수부만큼 완충(단일 SKU, 1.00 PLT) 파레트를 먼저 만든다.
   - 남는 소수부(remainder)는 여러 SKU를 묶어서 혼적 파레트로 구성한다.
   - 혼적 파레트(SKU 2개 이상)는 절대 **0.8 PLT**를 넘을 수 없다 (하드 캡, 2026-07-12 기준 0.85→0.8로 변경). **2026-07-15부터 목표 범위는 0.7~0.8**(사용자 지정) — 가능한 한 이 범위 안으로 몰아넣는 게 이상적이고, 마지막에 도저히 옮길 곳 없는 자투리 파레트 1개 정도가 그보다 낮게 남는 건 정상.
     - 이유: 다양한 상품을 섞어 짤 때 실제 1PLT 높이를 초과하지 않기 위한 안전마진.
   - 완충(단일 SKU) 파레트는 정의상 1.00(100%)이 정상이며 이 캡이 적용되지 않는다.
   - **SKU 개수: 7개는 소프트 목표치, 10개가 진짜 하드 캡** (2026-07-15 변경). 처음엔 "7개가 기본 목표, 하드 캡 아님"이었는데, 병합 단계에서 SKU 제한을 완전히 무시하다 보니 한 파레트에 17개 SKU까지 몰리는 문제가 발견됨 → 사용자가 "1PLT에 10SKU 넘으면 안 됨"이라고 명확히 해서, **배치 단계(1·2단계)부터 병합·재배분까지 전 과정에서 10개를 진짜 상한**으로 고정함. (7은 이제 병합 1차 시도에서만 "가능하면 SKU 적게" 우선순위로 남아있음.)
   - **다양한 SKU를 최대한 섞어서 파레트 수 자체를 줄이는 게 우선** (2026-07-15, 사용자 명시) — 그래서 아래 1·2단계 배치 시점부터 7이 아니라 10(ABSOLUTE_MAX_SKU)까지 채운다.
   - **자투리를 "큰 것(≥0.1)"과 "작은 것(<0.1)"으로 나눠서 2단계로 채운다** (2026-07-13, v3.1 — Best-Fit만으로는 작은 조각들이 뒤쪽 파레트 1~2개에 몰려서 SKU는 다 찼는데 PLT는 텅 빈 파레트가 남는 문제가 있어서 추가 개선):
     1. 큰 자투리(0.1 이상)는 Best-Fit-Decreasing: 정렬 후 "들어갈 수 있는 기존 혼적 파레트 중 남는 공간이 가장 적은" 곳부터 채움. (SKU 캡 10 적용)
     2. 작은 자투리(0.1 미만)는 먼저 1)에서 만든 파레트들의 남는 gap에 Best-Fit으로 최대한 흡수시킴. (SKU 캡 10 적용)
     3. 그러고도 남는 작은 자투리는, 필요한 새 파레트 개수를 미리 계산(`총량÷0.8`과 `개수÷10` 중 큰 쪽)해서 그만큼의 새 파레트를 만들어놓고 **라운드로빈**으로 순서대로 나눠 담음 (한 파레트를 SKU 10개까지 다 채우고서야 다음으로 넘어가는 방식이 아님).
   - **4) 병합(consolidation) 단계**: 위 1~3단계 후에도 저충전(약한) 혼적 파레트가 여러 개 남으면 서로 합쳐서 개수를 줄인다. 1차는 "합친 SKU 수가 7개 이하가 되는 조합만" 합치고, 그래도 약한 파레트가 남으면 2차로 **SKU 10개까지는**(10 초과는 절대 금지) 합친다.
   - **5) 재배분(rebalance) 단계 (2026-07-15 신규 추가)**: 병합은 "파레트 통째로 두 개를 합칠 수 있을 때만" 작동해서, 두 저충전 파레트를 합치면 SKU가 10개를 넘어버리는 경우엔 둘 다 낮은 충전률로 남아있는 문제가 있었다 (예: 0.13/4SKU + 0.24/10SKU = 14SKU라 못 합쳐짐). 그래서 파레트 단위가 아니라 **낱개 상품(SKU) 단위**로 두 가지를 시도한다:
     1. 0.7 미만인 파레트의 상품을 다른 파레트의 남는 공간(SKU<10, fill≤0.8)으로 하나씩 옮긴다.
     2. 옮길 자리가 없으면(대상이 이미 SKU 10개라서 등) **이미 0.7 이상인 파레트와만** 상품 하나씩 맞바꾼다 — 그 파레트에서 "이 약한 파레트에 가장 크게 보탬이 되는"(occupancy가 가장 큰) 상품을 빼서 이 약한 파레트로 옮기고, 대신 약한 파레트의 작은 상품을 그 자리에 채워 넣는 식(사용자가 준 예시: "PLT21에서 111691을 빼고 PLT19 물량을 넣는" 방식). 약한 파레트끼리 맞바꾸는 건 의미가 없어서 제외.
     - 완전히 비워진 파레트는 없앤다(파레트 수가 줄어듦). 마지막에 도저히 옮길 곳이 없는 자투리 하나 정도가 낮은 충전률로 남는 건 정상.
     - **주의**: 초기 구현에 루프 종료 조건 버그가 있어 상품이 중복되거나 유실되는(낱개수 총합 불일치) 문제가 있었음 — `swapped` 플래그로 수정 완료, 재검증 시 항상 낱개수 총합이 정확히 보존되는지 확인할 것.
     - 실측 검증: 65건 실데이터에서 혼적 파레트 11개 중 10개가 0.71~0.80, 65건 다른 조합에서도 14개 중 13개가 0.72~0.80로 재현됨(패턴이 데이터셋에 상관없이 일반적으로 성립). 낱개수 총합은 재배분 전후 항상 정확히 일치(예: 25,114개=25,114개).
   - **완충 파레트 1개당 낱개수 계산 (2026-07-15 갱신, v4)**: 박스당 PLT 수(cpBox)는 **재적 마스터관리가 등록돼 있으면 그게 우선**(쿠팡 기준, 실제 재적 기준), 없으면 단위 기준표의 PLT당박스량. 박스당 낱개수는 항상 단위 기준표(같은 상품코드)에서 가져옴(재적 마스터관리엔 이 정보가 없음). `유효cpBox × 박스당낱개수`로 완충파레트 낱개수를 정확히 계산. 어느 쪽도 없으면 `Math.round(상품별 총낱개 ÷ 총occupancy)`로 근사. 잔여 파레트 몫 = `상품별 총낱개 - (완충파레트 낱개수 × 완충파레트 개수)`로 역산해서 항상 정수+오차 없이 합계가 정확히 맞아떨어짐 (절대 소수점 안 나옴).
     - 실측 검증(재적 마스터관리 매칭 시, 2026-07-15): 193401 — 입력 PLT=1(정상값)이었지만 재적마스터 쿠팡박스수=56 등록돼 있어서 `70박스÷56=1.25`로 재계산, 완충파레트 56×12(단위기준표 박스당낱개수)=672개 정확 + 잔여 168개(0.25 PLT) = 840개 정확 일치.
     - 실측 검증(단위 기준표만 매칭 시): PLT당박스량 56 × 박스당낱개수 12 = 완충파레트 672개 정확 (113787, 총낱개 780 → 672+108, 사용자가 직접 준 예시와 일치).
     - 단위 기준표에도 없을 때 폴백 실측: 총낱개 1680 ÷ PLT 1.15 → `Math.round`로 완충파레트 1461개(근사치, 어쩔 수 없음).
6. 결과 화면: 파레트별로 PLT 소수값(예: 0.50)으로 표시 (퍼센트 아님). 상단 통계 카드(2026-07-15 확장): 총 파레트 수 / **재적 전 총 PLT**(`rawPltSum` — 상차리스트에 실제 입력된 PLT 원본값 그대로의 합, 재적 마스터관리/단위 기준표 보정 전) / **재적 후 총 PLT(합산)**(실제 pallets의 fill 합, 보정 후) / 이론상 최소 파레트 수(`Math.ceil(재적후총PLT)`) / **낱개 총합**(productAgg의 totalUnits 합) / 완충 파레트 수 / 혼적 파레트 수.
   - **주의(2026-07-15 수정)**: 처음엔 "재적 전"도 `productAgg.totalOccupancy`(이미 보정된 값)로 계산해서 재적 전/후가 항상 똑같이 나오는 버그가 있었음 — 사용자가 "재적전은 그냥 내가 입력한 PLT값을 더한 합계여야지" 라고 지적해서 `rawPltSum`(순수 입력값 합)으로 분리함. 재적 마스터관리로 보정되는 상품이 있으면 이제 재적 전/후가 실제로 달라짐 (예: 193401 입력 PLT 1.00 → 보정 후 1.25).
7. **기본 화면**: 앱 진입 시 "상차리스트 입력 & 계산" 탭이 먼저 보인다. 나머지 두 탭("단위 기준표", "재적 마스터관리")은 보조 탭.
8. **거래처 선택 (2026-07-13 추가, 2026-07-15 완료 워크플로 확장)**: "상차리스트 불러오기" 클릭 시 그리드 전체를 `fullShipment`로 파싱한 뒤, `vendor`(고객사&거래처명) 값의 종류가 2개 이상이면 계산으로 바로 안 가고 `#calc-vendor-view`(거래처 선택 화면)를 먼저 보여준다(이때 전체 거래처 목록을 `allVendors`에 저장). 목록에서 거래처를 하나 클릭하면 `shipment = fullShipment.filter(그 거래처)`로 필터링해서 입력 화면(미리보기+계산 버튼)으로 돌아간다. 거래처가 1종류뿐이거나 비어있으면 선택 화면 없이 바로 진행.
   - **거래처별 완료 처리 (2026-07-15 추가)**: 파레트 상세 화면에서 마지막 파레트일 때만 "완료" 버튼이 나타남(`detail-complete-btn`). 누르면 `completeVendorAndAdvance()`가 실행되어: 현재 거래처를 `completedVendors`(Set)에 추가 → 아직 완료 안 된 다음 거래처가 있으면 그 거래처로 `selectVendor`+`runCalculation`까지 자동 실행해서 **그 거래처의 파레트 목록(결과) 화면**으로 바로 이동(파레트 1번 상세로 바로 들어가지 않음, 2026-07-15 사용자가 명확히 정정) / 남은 거래처가 없으면 거래처 선택 화면으로 돌아감.
   - 거래처 선택 화면에서 완료된 거래처는 회색(`opacity:0.5`)에 "완료" 뱃지가 붙고 클릭 안 됨(`renderVendorList`가 `completedVendors` 확인).
   - 결과 화면(파레트 목록)의 뒤로가기 버튼(`#results-back-btn`)은 `allVendors.length>1`(다중 거래처 세션)이면 "← 거래처 선택으로 돌아가기"로 라벨이 바뀌고 클릭 시 거래처 선택 화면으로 돌아감(`goBackFromResults()`) — 단일 거래처 세션이면 기존처럼 "← 입력으로 돌아가기". 새로 상차리스트를 불러오면 `completedVendors`는 초기화됨.
   - 여러 거래처를 한 번에 계산하는 기능은 없음(위 완료 워크플로로 하나씩 순서대로 처리).
9. **계산 탭 내부는 입력→(거래처 선택)→결과→상세 화면 전환** (2026-07-12부터, 한 페이지에 다 쌓이면 헷갈린다는 피드백 반영):
   - `#calc-input-view` (입력) → "적재 계산하기" 클릭 → `#calc-results-view` (결과: override/missingplt 카드 + 통계 + 파레트 한 줄짜리 리스트) → 파레트 행 클릭 → `#calc-detail-view` (해당 파레트 상세, 전체 너비 표라 바코드도 안 잘림).
   - 항상 한 번에 하나의 뷰만 보이며(showInputView/showResultsView/openPalletDetail/closePalletDetail로 전환), 각 화면에 뒤로가기 버튼이 있음. 새 브라우저 탭/페이지가 아니라 같은 탭 안에서 영역만 바뀌는 것.
   - 파레트 상세 화면엔 "← 이전 파레트/다음 파레트 →" 버튼과 "N / 총개수" 표시가 있어 목록으로 안 돌아가고 바로 옆 파레트로 넘어갈 수 있음 (경계에서는 버튼 비활성화, `currentDetailIdx` 전역변수로 위치 추적).
   - 파레트 목록(각 줄)에는 "SKU N개 · 총 M개"처럼 그 파레트의 총 낱개수 합계도 같이 보여줌 (2026-07-14 추가) — 예전엔 상세를 열어야만 낱개수가 보여서 "재적된 수량이 안 보인다"는 피드백이 있었음.
   - 파레트 상세 표의 BOX바코드/상품바코드는 뒤 5자리만 표시 (2026-07-15, `last5()` 헬퍼 사용) — 바코드 전체는 안 봐도 되고 뒤 5자리로 구분 가능하다는 사용자 피드백.
   - 파레트 상세 화면 상단에 현재 보고 있는 거래처명을 표시 (2026-07-15 추가, `#detail-vendor-note`, `shipment[0].vendor` 기준).
   - **Ctrl+Z / Ctrl+Y(맥 Cmd+Z / Cmd+Shift+Z)로 화면 단계를 키보드만으로 뒤로/앞으로 이동** (2026-07-15 추가). `showInputView`/`showVendorView`/`showResultsView`/`openPalletDetail`/`closePalletDetail`이 호출될 때마다 `recordNav()`가 `{view, detailIdx}`를 `navHistory` 배열에 쌓고(브라우저 히스토리와 동일한 방식, `navIndex`로 위치 추적), keydown 핸들러가 그 기록을 따라 이동만 함. 입력 칸(그리드 셀/텍스트박스)에 포커스가 있을 땐 개입하지 않아 원래의 "입력 취소" 동작을 안 건드림.
     - **주의**: `navHistory`는 화면 종류만 기록하고 그 시점의 `pallets`/`shipment` 데이터는 따로 저장 안 함 — `selectVendor()`(거래처 전환 시점)에서 반드시 `navHistory=[]; navIndex=-1;`로 초기화해야 함. 안 그러면 거래처 완료→다음 거래처 자동 이동 후 Ctrl+Z로 이전 거래처 있던 시점까지 되돌아갈 때, 화면은 "이전 거래처 결과"였던 자리로 가지만 실제론 이미 새 거래처로 덮어써진 `pallets`가 표시되는 화면-데이터 불일치가 생김 (2026-07-15, 배포 전에 발견/수정).
   - 파레트 상세 표엔 "박스수" 열도 있음 (2026-07-15 추가): 단위 기준표에 등록된 상품은 그 박스당낱개수로, 없는 상품은 상차리스트에 입력된 BOX입수 값으로 대신 계산 (`aggMap`에 상품별 `unitsPerBox`도 같이 집계). 둘 다 없으면 '-' 표시.
   - 스크롤 위치 규칙 (2026-07-12): "상차리스트 불러오기" 클릭 시 미리보기 카드로 스크롤(scrollIntoView) / "적재 계산하기" 클릭 시 결과 화면 맨 위로 스크롤(showResultsView 내부) / 파레트 상세 진입 시에도 맨 위로 / "← 목록으로" 클릭 시엔 맨 위가 아니라 방금 보던 파레트 행(`.pallet-row[data-idx]`)으로 스크롤. 전부 `behavior:'auto'` 사용(smooth는 이 환경 자동화 테스트에서 타이밍이 불안정해서 피함).

## 지금까지의 진행 상황
- v1: 마스터관리 탭 + 상차리스트 업로드/붙여넣기 탭 + 계산 결과(파레트 카드, 펼치기) 구현 완료.
- v2 (2026-07-12): 상차리스트에 PLT 열 추가, 마스터를 보조/override 전용으로 전환, 기본 탭을 상차리스트 입력으로 변경. 로컬 HTTP 서버로 override/pass-through/PLT누락 3가지 케이스 모두 브라우저 테스트 완료 (정상 동작 확인).
- 혼적 파레트 하드캡 0.85 → 0.8로 변경 (2026-07-12).
- 외관을 미니멀/모던 스타일로 리디자인 (CSS 변수 값만 조정, 변수명/JS 로직은 그대로 유지) (2026-07-12).
- GitHub Pages 배포 완료, window.storage → localStorage 전환 (2026-07-12).
- 상차리스트 입력을 textarea+파일업로드 → 엑셀 붙여넣기 전용 스프레드시트 그리드로 전면 교체, SheetJS/파일업로드 제거 (2026-07-12).
- 파레트 상세 표에 188/88바코드 열 추가 (2026-07-12).
- 상차리스트 "수량" 라벨을 "박스수" → "낱개 수(EA)"로 수정 (2026-07-12). 마스터 override 공식의 단위 정합성은 아직 미해결 (위 도메인 규칙 1번 참고).
- 결과 화면을 카드 그리드(펼치기 방식) → 입력/결과/상세 3단계 화면 전환 구조로 전면 개편, 파레트 상세는 전체 너비로 표시해 바코드 잘림 문제 해결 (2026-07-12).
- 파레트 상세 화면에 이전/다음 파레트 이동 버튼 추가 (2026-07-12).
- 마스터리스트 탭 "이 브라우저에만 저장됩니다" 안내 문구 제거 (2026-07-12).
- 마스터/상차리스트 카드의 설명 문구(.sub) 제거 — 사용자가 이미 숙지해서 불필요 (2026-07-12).
- 상차리스트 입력 그리드 열 너비 조정: 상품명 200px로 축소, 88바코드 110→140px로 확대 (188바코드와 균형, 잘림 방지) (2026-07-12).
- 브랜딩/UX 정리 (2026-07-12): 타이틀 "트럭적재 자동구성"→"쿠팡계산기", 브랜드 아이콘(SVG mark) 제거, "+20행 추가"→"+ 행 추가"(1행씩), "🚚 적재 계산하기"→"적재 계산하기"(아이콘 제거), 마스터탭 "예시 데이터 채워보기" 버튼과 가져오기 안내문 제거, 마스터 "+ 행 추가" 클릭 시 새 행으로 자동 스크롤(scrollIntoView) 추가.
- **v3 (2026-07-13, 쿠팡 피드백 반영)**:
  - 상차리스트 입력 8열로 확장(고객사&거래처명/박스수 추가), 마스터 override 계산을 낱개수→박스수 기준으로 수정해 단위 불일치 해소.
  - 거래처 선택 화면(`#calc-vendor-view`) 추가 — 여러 거래처가 섞여 있으면 계산 전에 하나 선택.
  - 혼적 파레트 SKU 최대 10→7, 패킹 알고리즘 First-Fit-Decreasing→Best-Fit-Decreasing으로 교체 (뒷부분 자투리 파레트 몰림 문제 완화, 실측 테스트로 검증).
  - Firebase 공유 저장소 전환은 사용자가 "너무 복잡하다"며 취소 (2026-07-13). localStorage(브라우저별 저장)로 확정, 다른 컴퓨터에서 마스터리스트 안 보이는 건 정상 동작.
- v3.1 (2026-07-13): 혼적 파레트 뒷부분에 약한(저충전) 파레트가 2개 이상 몰리는 문제 개선 — 자투리를 큰것/작은것으로 나누고, 작은 것은 기존 gap 흡수 후 남는 건 라운드로빈으로 분산. 위 도메인 규칙 4번 참고.
- v3.2 (2026-07-13): 상차리스트 입력을 10열(BOX입수/BOX/EA/총낱개로 세분화)로 확정. 총낱개가 항상 정수가 되도록 파레트 배분 로직을 "완충파레트당 반올림한 낱개수 고정 + 잔여는 역산" 방식으로 변경 (기존엔 소수점 낱개수가 나오는 버그가 있었음). 혼적 파레트 SKU 7개 제한을 하드캡→소프트 기준으로 변경하고, 저충전 파레트가 남으면 SKU 제한을 넘겨서라도 합치는 병합 단계 추가 (0.8 fill 캡은 항상 유지). 로컬 서버로 충분히 검증 후 배포.
- v3.3 (2026-07-13): 사용자가 실제 회사 마스터데이터(~1200행, 4열: 상품코드/상품명/쿠팡박스수·PLT/BOX당낱개수)를 붙여넣기로 결정 → 마스터 스키마를 여기 맞춰 변경 (`ownBox` 필드 제거, `unitsPerBox` 필드 신설). 실데이터 샘플(부침가루1kg 등)과 300행 대량 붙여넣기로 컬럼 매핑·override 계산·성능(300행 24ms) 전부 검증 완료 후 배포. **⚠️ v3.4에서 이 판단이 틀린 것으로 정정됨 — 아래 참고.**
- **v3.4 (2026-07-14)**: 사용자가 명확히 정정 — "재적 마스터관리"는 원래 목적(입고 파레트→쿠팡 출고 변환)이 따로 있고, 1200행 데이터는 그것과 무관한 "전 상품 단위 기준표"라는 별개 개념. 그래서:
  - "재적 마스터관리" 탭을 원래 4열 스키마(상품코드/상품명/박스수·PLT 자사참고용/쿠팡박스수·PLT)로 복원하고 계산 로직에서 완전히 분리 (현재 미사용, 향후 별도 기능 대기).
  - 새로 **"단위 기준표"** 탭(`#panel-unitref`, `unitRef` 배열, `unit-reference-list` 키) 신설 — 상품코드/상품명/PLT당 박스량/박스당 낱개수 4열. `resolveItemPlt`의 override 계산(BOX÷PLT당박스량)이 이제 이 테이블을 참조 (`findUnitRefFor`). 이 표에 전 상품(~1200개)을 채우면, 상차리스트의 반올림된 PLT 대신 항상 정밀 계산된 값을 씀 — "1680→1461 아니라 1470" 문제의 근본 해결책.
  - 빈 상차리스트 행은 이미 자동 무시되고 있었음을 재확인 (추가 수정 불필요).
  - 실데이터 샘플로 override 정밀도(165÷70=2.3571, 700+700+250=1650 정확), pass-through 미매칭 정상 동작, 빈 행 무시 전부 재검증 후 배포.
  - **⚠️ v3.4는 "BOX÷PLT당박스량으로 PLT를 덮어쓴다"는 override 개념을 그대로 유지한 채 이름만 바꾼 것이었는데, 이것도 틀렸음이 v3.5에서 다시 정정됨.**
- **v3.5 (2026-07-14, 재정정)**: 사용자가 다시 명확히 함 — "INPUT의 PLT는 기준값이라 절대 변경될 수 없다." 단위 기준표를 만든 진짜 목적은 PLT를 재계산하는 게 아니라, "총낱개에 소수점이 나오면 안 되니까 1PLT가 정확히 몇 개인지"를 알아내기 위함이었음 (역산은 PLT를 바꾸는 게 아니라 정수 단위 기준값을 찾는 것). 그래서:
  - `resolveItemPlt`를 순수 `parseFloat(item.plt)` pass-through로 되돌림 — 어떤 데이터(재적 마스터든 단위 기준표든)도 PLT를 덮어쓰지 않음.
  - override 관련 UI(뱃지 "단위 기준표로 정밀 재계산됨", `#override-card`, `overrideItems` 변수, `renderOverrideInfo` 함수) 전부 삭제. 사용자 요청대로 "적용됐다"는 표시 자체를 안 함 — 그냥 조용히 정확한 파레트 결과만 보여줌.
  - 완충파레트 낱개수 계산(runCalculation 내부)에서 `findUnitRefFor`로 단위 기준표를 찾아 `cpBox × unitsPerBox`를 쓰도록 변경 (기준표에 없으면 기존 `Math.round(총낱개÷occ)` 폴백). PLT/occupancy 자체는 항상 입력값 그대로.
  - 실측 재검증: 단위 기준표에 W001(cpBox=70, unitsPerBox=21) 등록 + 입력 PLT=1.15(그대로 유지) → 완충 1470개 + 잔여 210개(fill 0.15, PLT 그대로) = 1680 정확. 기준표에 없는 상품은 폴백대로 1461/219 근사치, override 카드 없음, 빈 행 무시 재확인.
- **v3.6 (2026-07-14, 버그 수정)**: 사용자가 실데이터(214063 얼티브 피스타치오 등)로 테스트하다 총낱개가 0으로 나오는 버그 발견. 원인: 엑셀에서 복사한 숫자에 천단위 콤마가 붙어있으면(`"4,716"`) `Number()`/`parseFloat()`가 `NaN`을 반환하고 `NaN||0`이 `0`이 되어 조용히 0으로 처리되던 버그. 부수적으로 단위 기준표의 `cpBox`(PLT당박스량) 필드가 붙여넣기/수동수정 시 `Number()`로 강제 변환되던 버그도 같이 발견 (원래 "48 (8방*6단)" 같은 텍스트 섞인 값을 받아야 하는 필드라 문자열로 유지해야 함).
  - `parseNum()` 헬퍼 함수 신설(콤마 제거 후 `Number()`) — `importShipGrid`/`importUnitRefPaste`/`importMasterPaste`/`updateMaster`/`updateUnitRef`/`resolveItemPlt`/`cpBoxNum` 전부 이걸 쓰도록 통일.
  - `unitRef`/`master`의 `cpBox` 필드는 파�스/수동수정 어디서도 더 이상 `Number()`로 강제 변환 안 하고 항상 문자열로 유지 (박스량+구성설명 텍스트를 받을 수 있어야 하므로).
  - 실측 재검증: 사용자가 준 원본 그대로(콤마 포함 "4,032"/"4,716", 지수표기 바코드 포함) paste 이벤트로 테스트 → 213853(56×36=2016×2파레트=4032 정확), 214063(160×18=2880 완충+1836 잔여=4716 정확) 모두 정상.
  - 참고(코드로 못 고치는 부분): 바코드 열이 `1.88014E+13`처럼 지수표기로 붙여넣어짐 — 이건 엑셀에서 해당 열을 "텍스트" 서식으로 바꾸지 않고 복사해서 원본 자리수가 이미 유실된 것. 사용자에게 엑셀에서 바코드 열 서식을 텍스트로 바꾼 뒤 복사하라고 안내 필요.
- **v3.7 (2026-07-14)**: 사용자가 실제 안성8 FC/용인1(상) 거래처 데이터로 테스트하다, PLT가 반올림되어 0으로 들어온 상품들(193233/193401/202633/207231/215744/219746)이 파레트 결과에서 통째로 사라지는 걸 발견 (계산 오류는 아니고 "PLT=0=점유공간 0"이라 정상 동작이었지만, 사용자 입장에선 데이터가 사라진 것처럼 보임). 사용자가 "재적이 가장 우선"이라고 명확히 하면서 v3.5/v3.6의 순수 pass-through 원칙을 재수정:
  - `resolveItemPlt`: PLT>0이면 그대로 사용(안 바뀜, 원칙 유지) / PLT<=0·빈값이면 단위 기준표에서 `BOX÷PLT당박스량`으로 재적 복구 시도 / 그래도 안 되면 그때만 "PLT 값 누락"으로 제외.
  - 실측 재검증: 안성8 FC 9개 상품 전부(PLT=0 6개 포함) 5개 파레트에 빠짐없이 반영, 낱개수 전부 정수, missingPltItems=0.
  - **아래 v4에서 해결됨**: 상품코드/거래처명 없이 수량만 있는 "빈 줄"(`264 0 3696 5` 형태) 처리 방침이 확정됨 — forward-fill이 아니라 "행 전체 유효성 검사"로 해결.
- **v4 (2026-07-15, 대규모 정정)**: 사용자가 실제 다운로드 파일 특성과 재적 마스터관리의 진짜 의미를 설명하며 세 가지를 정정:
  1. **빈 줄 처리**: 상차리스트 다운로드 파일엔 상품코드 등이 빈 "합계행"이 맨 아래 붙는 경우가 있어, 이런 행이 계산에 안 섞이게 **10개 열 중 하나라도 빈칸이면 그 행 전체를 제외**하도록 변경 (`isCompleteShipRow`, 위 규칙 1번). 이전에 시도했던 "코드 없는 행이 위 행 코드를 이어받는(forward-fill)" 방식은 폐기 — 사용자가 그냥 제외를 원함.
  2. **재적 마스터관리가 계산에 참여하게 됨** (위 규칙 2, 4번): 단위 기준표(="우리 센터가 지금 쌓는 방식")와 재적 마스터관리(="쿠팡 납품을 위해 실제로 다시 쌓아야 하는 방식")가 서로 다른 기준이라는 걸 사용자가 설명 — 재적 마스터관리에 등록된 상품은 입력 PLT가 정상값이어도 `BOX÷재적마스터.쿠팡박스수`로 무조건 재계산됨. 즉 "유효한 입력 PLT는 절대 안 바뀐다"는 원칙이 **재적 마스터관리에 등록 안 된 상품에 한해서만** 적용되는 것으로 최종 정리됨.
  3. **비고 열에 "재적값 사용"(보라색) 뱃지 추가**: 재적 마스터관리 또는 단위 기준표 fallback으로 값이 채워진 경우와 순수 입력값(초록 "입력값 사용")을 구분해서 보여줌 — 이전엔 구분 없이 전부 "입력값 사용"으로 보이던 버그.
  - 실측 검증: 193401(재적마스터 쿠팡박스수=56 등록) — 70박스·입력PLT=1(정상값)인데도 1.25로 재계산, 완충 672개(56×12)+잔여 168개=840 정확. 재적마스터 없는 상품(113787 등)은 기존 v3.7 fallback/pass-through 그대로 정상 동작 재확인.
  - PLT 화면 표시 자릿수도 소수 4자리→2자리로 축소 (`fmtPlt`).
- **v5 (2026-07-15, 파레트 패킹 최적화)**: 사용자가 실제 65건 데이터(동탄1 거래처)로 테스트하며 발견/요청:
  1. 병합 단계가 SKU 제한을 완전히 무시해서 한 파레트에 17개 SKU까지 몰리는 문제 발견 → **SKU 10개를 진짜 하드 캡으로 고정**(`ABSOLUTE_MAX_SKU`), 배치·병합 전 단계에 적용 (위 규칙 5번).
  2. "다양한 SKU를 최대한 섞어서 파레트 수를 줄여라" 요청 → 배치 단계부터 7이 아니라 10까지 채우도록 변경, 이론상 최소 파레트 수와의 격차 축소 확인 (31→30→29개, 이론치 27).
  3. "혼적 파레트는 전부 0.7~0.85 사이(가능하면 0.8에 가깝게), 마지막 자투리 하나만 예외"라는 목표 범위 요청 → **재배분(rebalance) + 맞바꾸기(swap) 단계 신규 추가** (위 규칙 5번). 사용자가 구체 예시(PLT21에서 111691 빼고 PLT19 물량 넣기)를 줘서 그 방향으로 구현.
  4. 구현 중 swap 루프 종료조건 버그로 낱개수 총합이 깨지는 사고 발생(25114→24332) → `swapped` 플래그로 즉시 수정, 재검증으로 총합 보존 재확인. **이런 종류의 재배치/최적화 코드는 반드시 "낱개수 총합 보존" 체크를 실측으로 재확인할 것.**
  5. 결과 화면에 "재적 전 총 PLT", "재적 후 총 PLT(합산)", "낱개 총합", "이론상 최소 파레트 수" 통계 4종 추가 (위 규칙 6번) — 재적 전/후 PLT가 항상 일치해야 물량 손실이 없다는 걸 사용자가 바로 확인할 수 있게 함.
  - 실측 검증: 65건 데이터(동탄1)와 60건 데이터(별도 테스트셋) 둘 다 혼적 파레트 대부분이 0.7~0.8 사이로 재배치되고, 마지막 1개만 예외로 낮게 남는 패턴이 일관되게 재현됨. 낱개수 총합은 재배치 전후 항상 정확히 일치.
- 다음에 요청받을 수정사항은 사용자와 대화하며 확인 후 반영할 것. 임의로 도메인 규칙(위 1~9번)을 바꾸지 말고, 바뀔 경우 반드시 사용자에게 먼저 확인. **PLT 결정 로직은 v4가 최종본(위 도메인 규칙 4번) — 재적 마스터관리 > 유효 입력 PLT > 단위 기준표 fallback > 제외, 순서로 우선순위가 있음. 파레트 패킹은 v5가 최종본(위 도메인 규칙 5번) — SKU 10개 하드캡, 혼적 파레트는 최대한 0.7~0.8 사이로 재배분. "재적 마스터관리"(쿠팡 기준, 계산에 참여)와 "단위 기준표"(자사 기준, 낱개수 계산 보조)는 역할이 다르니 혼동하지 말 것. 숫자 파싱은 항상 `parseNum()`을 써서 콤마를 방어할 것. 재배치 로직을 건드릴 땐 낱개수 총합 보존을 항상 재검증할 것.** 코드를 고치면 반드시 git commit + push까지 해야 실제 배포 사이트에 반영됨.
